{
  "configurations": [
    {
      "name": "Launch Brave",
      "request": "launch",
      "type": "pwa-chrome",
      "url": "http://localhost:3000/${relativeFileDirname}",
      "webRoot": "${workspaceFolder}",
      "runtimeExecutable": "/usr/bin/brave"
    }
  ]
}